{
  "name": "landing-page",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/landing-page",
  "projectType": "application",
  "tags": [],
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/.open-next"],
      "options": {
        "cwd": "apps/landing-page",
        "command": "next build --webpack && opennextjs-cloudflare build -s && opennextjs-cloudflare deploy"
      }
    }
  }
}
